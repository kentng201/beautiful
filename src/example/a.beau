set age to 20
set weight to 50
set total to age + weight

., set test to new decimal array 1 2

if 2 or 3
    true

if total less than or equal 100 and total not full equal 200
    console.log total
    console.log 100
    set hi to 200
else if total is int
    console.log total

every user in usrs 
where (user.age equal 20)
    console.log i
    set i to i + 1
    set user from User 
    select username 
    where id == 1

